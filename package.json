{
  "name": "elective-course-selector",
  "version": "1.0.0",
  "description": "MCA Elective Course Selector - Full Stack Application",
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install --include=dev && npm run build",
    "install-all": "cd backend && npm install && cd ../frontend && npm install --include=dev",
    "render-build": "npm run install-all && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["mca", "elective", "course", "selector"],
  "author": "MCA DGV College",
  "license": "ISC"
}
